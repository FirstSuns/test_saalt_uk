<section id="section-{{ section.id }}" data-section-id="{{ section.id }}" data-section-type="introduction">
  <div class="section-inner">
    <div class="top-content">
      <div class="top-image">
        <img class="Image--lazyLoad hidden-phone"
          src="{{ section.settings.image | img_url: '1x1' }}"
          data-src="{{ section.settings.image | img_url: 'master' }}"
          alt="{{ section.settings.image.alt | escape }}">
        <img class="Image--lazyLoad hidden-tablet-and-up"
          src="{{ section.settings.image | img_url: '1x1' }}"
          data-src="{{ section.settings.image | img_url: 'x120' }}"
          alt="{{ section.settings.image.alt | escape }}">
      </div>

      <h2 class="section-title Heading">{{ section.settings.title }}</h2>
      <div class="section-descr">{{ section.settings.description }}</div>
      <h3 class="section-subtitle">{{ section.settings.subtitle }}</h3>
    </div>

    {% if section.blocks.size > 0 %}
    <div class="item-list">
      {% for block in section.blocks %}
      <div class="item">
        <div class="item-inner">
          <div class="item-image">
            <img class="Image--lazyLoad hidden-phone"
              src="{{ block.settings.image | img_url: '1x1' }}"
              data-src="{{ block.settings.image | img_url: 'master' }}"
              alt="{{ block.settings.image.alt | escape }}">
            <img class="Image--lazyLoad hidden-tablet-and-up"
              src="{{ block.settings.image | img_url: '1x1' }}"
              data-src="{{ block.settings.image | img_url: 'x120' }}"
              alt="{{ block.settings.image.alt | escape }}">
          </div>
          <h4 class="item-title">{{ block.settings.title }}</h4>
          <div class="item-descr">{{ block.settings.description }}</div>
        </div>
      </div>
      {% endfor %}
    </div>
    {% endif %}
  </div>
</section>

{% schema %}
  {
    "name": "Introduction",
    "class": "section--introduction",
    "max_blocks": 4,
    "settings": [
      {
        "type": "image_picker",
        "id": "image",
        "label": "Image"
      },
      {
        "type": "text",
        "id": "title",
        "label": "Title"
      },
      {
        "type": "textarea",
        "id": "description",
        "label": "Description"
      },
      {
        "type": "text",
        "id": "subtitle",
        "label": "Subtitle"
      }
    ],
    "blocks": [
      {
        "type": "image",
        "name": "Item",
        "settings": [
          {
            "type": "image_picker",
            "id": "image",
            "label": "Image"
          },
          {
            "type": "text",
            "id": "title",
            "label": "Title"
          },
          {
            "type": "textarea",
            "id": "description",
            "label": "Description"
          }
        ]
      }
    ],
    "presets": [
      {
        "category": "Image",
        "name": "Introduction",
        "settings": {}
      }
    ]
  }
{% endschema %}